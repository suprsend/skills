name: suprsend-docs-support
description: >
  How to access SuprSend documentation and get support. Includes docs-over-SSH
  access (ssh suprsend.sh), the .md-suffix URL convention for raw markdown,
  LLM-friendly bulk endpoints, in-app chat, AI copilot, Slack community, and
  email support. Use when the user asks where to find SuprSend docs, how to
  get support, or when the agent needs to look up SuprSend concepts
  programmatically.
metadata:
  author: suprsend
  category: support

sources:
  - type: static
    key: content
    path: content.md
