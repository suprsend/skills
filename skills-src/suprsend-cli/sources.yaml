name: suprsend-cli
description: >
  SuprSend CLI reference for AI agents. Use when working with SuprSend
  workflows, templates, channels, or API integrations. Covers all CLI
  commands, configuration, and common patterns.
license: MIT
metadata:
  author: suprsend
  version: "1.0"

sources:
  - type: static
    key: overview
    path: overview.md
  - type: static
    key: commands
    path: commands.md
  - type: static
    key: examples
    path: examples.md

references:
  - filename: troubleshooting.md

scripts:
  - filename: check-auth.sh

assets:
  - filename: workflow-template.json
